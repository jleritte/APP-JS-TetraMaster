const entities = new WeakMap()

export default class Player{
  constructor(name) {
    entities.set(this, {
      id,
      name,
      collection
    })
  }
}

// Helper Functions
function generateId() {

}
